package layouts.Grid4

import graph.FactoryNode
import graph.Graph
import graph.Node

class Grid4Factory(private val graph: Graph, tag: String) : FactoryNode(tag) {

    override fun getIncoming(): List<Node> {
        return super.getIncoming()
    }
}